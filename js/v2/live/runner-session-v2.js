/* MILITOPO V2 · F3A · Runner Home + sesión Live V2.
   La home usa el MISMO singleton Firebase/Auth que el resto de MILITOPO.
   Evita inicializar un segundo Auth y muestra errores recuperables en vez de quedarse cargando. */
import "../bootstrap.js?v=v2-f3a-runner-sessionfix-20260923";
import { onAuthStateChanged } from "https://www.gstatic.com/firebasejs/12.19.0/firebase-auth.js";
import { doc, getDoc } from "https://www.gstatic.com/firebasejs/12.19.0/firebase-firestore.js";
import { httpsCallable } from "https://www.gstatic.com/firebasejs/12.19.0/firebase-functions.js";
import { ref, get, onValue, update, onDisconnect, serverTimestamp } from "https://www.gstatic.com/firebasejs/12.19.0/firebase-database.js";

const STATUS_ES={prepared:"PREPARADO",published:"PUBLICADO",live:"EN DIRECTO",finished:"FINALIZADO"};
const state={services:null,user:null,events:[],selected:null,runId:"",unsubRun:null,unsubParticipant:null,heartbeat:null,panel:null,list:null,status:null,home:false,authSettled:false,loadSeq:0};

function esc(v){return String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));}
async function services(){if(!state.services)state.services=await globalThis.MILITOPO_V2.firebase();return state.services;}
function setText(id,text){const n=document.getElementById(id);if(n)n.textContent=text;}
function setStatus(text){ensurePanel();if(state.status)state.status.textContent=text;}
function injectStyle(){if(document.getElementById("m2RunnerV2Style"))return;const s=document.createElement("style");s.id="m2RunnerV2Style";s.textContent=`
.m2-runner-v2{margin:0 0 16px;padding:15px;border-radius:22px;border:1px solid rgba(126,220,150,.34);background:linear-gradient(180deg,rgba(8,29,15,.96),rgba(5,18,9,.97));color:#f5e6c8}
.m2-runner-v2 h2{margin:0 0 8px;font-size:1rem;color:#e6f6d7;letter-spacing:.06em}.m2-runner-v2-status{font-size:.78rem;line-height:1.45;opacity:.88;margin-bottom:10px}.m2-runner-v2-list{display:grid;gap:8px}.m2-runner-v2-event{padding:11px;border:1px solid rgba(255,255,255,.1);border-radius:14px;background:rgba(255,255,255,.035)}
.m2-runner-v2-event strong{display:block;font-size:.88rem}.m2-runner-v2-meta{font-size:.7rem;opacity:.7;margin-top:3px}.m2-runner-v2-btn{width:100%;min-height:44px;margin-top:8px;border:1px solid rgba(126,220,150,.4);border-radius:12px;background:rgba(126,220,150,.14);color:#efffe8;font:inherit;font-weight:900}.m2-runner-v2-btn:disabled{opacity:.45}.m2-runner-v2-live{margin-top:8px;padding:9px 10px;border-radius:12px;background:rgba(126,220,150,.12);border:1px solid rgba(126,220,150,.28);font-size:.75rem;font-weight:900;color:#dff7d3}`;document.head.appendChild(s);}
function ensurePanel(){
  if(state.home){
    const staticPanel=document.getElementById("m2RunnerHomePanel");
    if(staticPanel){state.panel=staticPanel;state.status=document.getElementById("m2RunnerHomeStatus");state.list=document.getElementById("m2RunnerHomeList");return staticPanel;}
  }
  if(state.panel?.isConnected)return state.panel;injectStyle();const hero=document.querySelector(".participant-main-hero");if(!hero)return null;
  const panel=document.createElement("section");panel.id="m2RunnerLiveV2";panel.className="m2-runner-v2";panel.innerHTML=`<h2>📡 LIVE V2 · MI CARRERA</h2><div id="m2RunnerV2Status" class="m2-runner-v2-status">Comprobando sesión…</div><div id="m2RunnerV2List" class="m2-runner-v2-list"></div>`;hero.insertAdjacentElement("afterend",panel);state.panel=panel;state.status=panel.querySelector("#m2RunnerV2Status");state.list=panel.querySelector("#m2RunnerV2List");return panel;
}
function bindListClicks(){
  if(!state.list||state.list.dataset.m2Bound==="1")return;state.list.dataset.m2Bound="1";
  state.list.addEventListener("click",e=>{const b=e.target.closest("[data-event-id]");if(!b)return;const eventId=String(b.dataset.eventId||"");if(state.home){try{sessionStorage.setItem("militopo_v2_runner_selected_event",eventId)}catch(_){}const target=new URL("runner.html",window.location.href);target.searchParams.set("app","1");target.searchParams.set("event",eventId);window.location.href=target.href;return;}connectEvent(eventId);});
}
function showSignedOut(message="Tu sesión no está activa en esta página."){
  setText("m2RunnerIdentityName","Sesión no detectada");setText("m2RunnerIdentityMeta","Vuelve a iniciar sesión para cargar tus carreras.");setStatus(message);
  if(state.list)state.list.innerHTML=`<a href="../../" style="display:grid;place-items:center;min-height:46px;border-radius:14px;border:1px solid rgba(240,193,106,.38);color:#fff3d7;text-decoration:none;font-weight:900">VOLVER A INICIAR SESIÓN</a>`;
}
async function renderIdentity(user){
  const {firestore}=await services();let profile={};
  try{const snap=await getDoc(doc(firestore,"users",user.uid));if(snap.exists())profile=snap.data()||{};}catch(error){console.warn("[MILITOPO runner profile]",error);}
  const displayName=String(profile.displayName||user.displayName||user.email||"Corredor").trim();const username=String(profile.usernameKey||profile.username||"").trim().toLowerCase();
  setText("m2RunnerIdentityName",displayName);setText("m2RunnerIdentityMeta",`${username?`@${username} · `:""}runner · ${user.email||""}`);
  const accountBtn=document.getElementById("m2RunnerOpenAccount");if(accountBtn&&!accountBtn.dataset.m2Bound){accountBtn.dataset.m2Bound="1";accountBtn.addEventListener("click",()=>{const btn=document.getElementById("m2AuthAccountBtn");if(btn){btn.click();return;}window.location.href="../../";});}
}
function eventCard(ev){const status=STATUS_ES[String(ev.status||"").toLowerCase()]||String(ev.status||"").toUpperCase();const live=String(ev.status||"").toLowerCase()==="live"&&String(ev.liveRunId||"").trim();const action=live?`<button type="button" class="m2-runner-v2-btn" data-event-id="${esc(ev.eventId)}">ENTRAR EN LA CARRERA</button>`:`<div class="m2-runner-v2-live">${String(ev.status||"").toLowerCase()==="finished"?"Carrera finalizada.":"Esperando a que el organizador inicie la carrera."}</div>`;return `<article class="m2-runner-v2-event"><strong>${esc(ev.eventName||"Carrera")}</strong><div class="m2-runner-v2-meta">${esc(status)} · ${esc(ev.eventId||"")}</div>${action}</article>`;}
function renderEvents(){ensurePanel();bindListClicks();if(!state.list)return;if(!state.user){showSignedOut();return;}if(!state.events.length){state.list.innerHTML="";setStatus("No tienes carreras preparadas, publicadas o en directo asociadas a esta cuenta.");return;}state.list.innerHTML=state.events.map(eventCard).join("");setStatus(`Tienes ${state.events.length} carrera${state.events.length===1?"":"s"} asociada${state.events.length===1?"":"s"} a tu cuenta.`);}
function cleanupConnection(){try{state.unsubRun?.();}catch(_){}try{state.unsubParticipant?.();}catch(_){}state.unsubRun=null;state.unsubParticipant=null;if(state.heartbeat){clearInterval(state.heartbeat);state.heartbeat=null;}state.runId="";}
async function markPresence(event,runId){const {database}=await services();const uid=state.user.uid;const pRef=ref(database,`v2/live/${event.ownerUid}/${event.eventId}/runs/${runId}/participants/${uid}`);const snap=await get(pRef);if(!snap.exists())throw new Error("Tu cuenta no está incluida en esta sesión Live.");const current=snap.val()||{};const nextStatus=["racing","started","finished"].includes(String(current.status||""))?String(current.status):"ready";await update(pRef,{online:true,status:nextStatus,lastSeen:serverTimestamp(),updatedAt:serverTimestamp()});try{await onDisconnect(pRef).update({online:false,lastSeen:serverTimestamp(),updatedAt:serverTimestamp()});}catch(_){}state.heartbeat=setInterval(()=>{update(pRef,{online:true,lastSeen:serverTimestamp(),updatedAt:serverTimestamp()}).catch(()=>{});},20000);state.unsubParticipant=onValue(pRef,s=>{const row=s.val()||{};const labels={ready:"PREPARADO",racing:"EN CARRERA",started:"EN CARRERA",finished:"FINALIZADO"};setStatus(`✅ Conectado a Live V2 · ${event.eventName} · ${labels[String(row.status||"ready")]||String(row.status||"ready").toUpperCase()}`);});}
async function connectEvent(eventId){const event=state.events.find(x=>x.eventId===eventId);if(!event)return;cleanupConnection();state.selected=event;setStatus("Conectando con la sesión Live V2…");try{const {database}=await services();const activeRef=ref(database,`v2/live/${event.ownerUid}/${event.eventId}/activeRun`);const snap=await get(activeRef);const active=snap.exists()?(snap.val()||{}):{};if(!active.runId||active.status!=="active")throw new Error("La carrera todavía no está en directo.");state.runId=String(active.runId);await markPresence(event,state.runId);state.unsubRun=onValue(activeRef,s=>{const row=s.val()||{};if(String(row.status||"")==="finished"){setStatus("🏁 La sesión Live V2 ha finalizado.");cleanupConnection();}});}catch(error){console.error("[MILITOPO F3A runner]",error);setStatus(`⚠️ ${String(error?.message||"No se pudo conectar a Live V2.")}`);}}
async function loadEvents(){if(!state.user)return;const seq=++state.loadSeq;setStatus("Buscando tus carreras asociadas…");try{const {functions}=await services();const call=httpsCallable(functions,"getRunnerLiveEvents");const res=await Promise.race([call({}),new Promise((_,reject)=>setTimeout(()=>reject(new Error("La consulta está tardando demasiado. Recarga la página.")),15000))]);if(seq!==state.loadSeq)return;state.events=Array.isArray(res.data?.events)?res.data.events:[];renderEvents();if(state.home)return;const params=new URLSearchParams(location.search||"");let requested=String(params.get("event")||"").trim();if(!requested){try{requested=String(sessionStorage.getItem("militopo_v2_runner_selected_event")||"").trim()}catch(_){}}const requestedEvent=requested?state.events.find(e=>e.eventId===requested&&e.status==="live"&&e.liveRunId):null;if(requestedEvent){setTimeout(()=>connectEvent(requestedEvent.eventId),180);return;}const live=state.events.filter(e=>e.status==="live"&&e.liveRunId);if(live.length===1)setTimeout(()=>connectEvent(live[0].eventId),350);}catch(error){console.error("[MILITOPO F3A events]",error);setStatus(`⚠️ ${String(error?.message||"No se pudieron consultar tus carreras ahora.")}`);if(state.list)state.list.innerHTML=`<button type="button" id="m2RunnerRetry" class="m2-runner-v2-btn">REINTENTAR</button>`;document.getElementById("m2RunnerRetry")?.addEventListener("click",()=>loadEvents(),{once:true});}}
async function handleUser(user){cleanupConnection();state.user=user||null;state.events=[];if(!user){showSignedOut();return;}if(!user.emailVerified){setText("m2RunnerIdentityName",user.displayName||user.email||"Corredor");setText("m2RunnerIdentityMeta",user.email||"");setStatus("Verifica tu correo desde MILITOPO antes de acceder a tus carreras.");return;}await renderIdentity(user);await loadEvents();}
async function init(){state.home=document.body?.dataset?.runnerHome==="1";ensurePanel();bindListClicks();setStatus("Recuperando tu sesión MILITOPO…");try{const {auth}=await services();if(typeof auth.authStateReady==="function"){try{await Promise.race([auth.authStateReady(),new Promise((_,reject)=>setTimeout(()=>reject(new Error("AUTH_READY_TIMEOUT")),8000))]);}catch(error){console.warn("[MILITOPO runner auth ready]",error);}}let delivered=false;const deliver=user=>{delivered=true;state.authSettled=true;handleUser(user).catch(error=>{console.error("[MILITOPO runner handle user]",error);setStatus("⚠️ No se pudo preparar tu área. Pulsa REINTENTAR.");});};onAuthStateChanged(auth,deliver,error=>{console.error("[MILITOPO runner auth]",error);showSignedOut("No se pudo recuperar la sesión de Firebase.");});if(auth.currentUser&&!delivered)deliver(auth.currentUser);setTimeout(()=>{if(!state.authSettled){if(auth.currentUser)deliver(auth.currentUser);else showSignedOut("La sesión no respondió. Vuelve a iniciar sesión.");}},10000);}catch(error){console.error("[MILITOPO F3A init]",error);showSignedOut("No se pudo iniciar el área del corredor. Recarga o inicia sesión de nuevo.");}}
init();
